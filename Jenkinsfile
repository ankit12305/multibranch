pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'

                sh 'mkdir qa'

               sh 'mkdir prod'

            }
        }
    }
}
